# ANKO GLOBAL v25 — обложка «Полимеры и реагенты»

Дата: 26.07.2026  
Основа: `anko-global-product-images-2026-07-26-v24.zip`

## Результат

Создана отдельная обложка раздела `category-cover-polimery`. Надпись
«Полимеры и реагенты» нанесена непосредственно на тёмно-зелёную этикетку
большой белой тары с учётом изгиба, перспективы и освещения.

Обложка подключена:

- в карточке раздела на главной странице;
- в карточке раздела общего каталога;
- как Open Graph-изображение страницы `category-polimer.php`;
- как изображение раздела в `sitemap.xml`.

Фотография конкретного товара `PolymersPlus VIS` сохранена без изменений и
продолжает использоваться только в карточке и на внутренней странице этого
товара.

## Адаптивные файлы

- `category-cover-polimery.jpg` — `1280×800`;
- `category-cover-polimery.webp` — `1280×800`;
- `category-cover-polimery-960.webp` — `960×600`;
- `category-cover-polimery-640.webp` — `640×400`.

## Проверка

- отдельный gate обложки: `passed`;
- четыре адаптивных файла декодируются и имеют ожидаемые размеры;
- все четыре варианта изображения товара `PolymersPlus VIS` совпадают с
  `v24` по SHA-256;
- независимый `php-parser@3.2.5`: 90 PHP-файлов, ошибок нет;
- статический аудит: 87 корневых PHP-файлов, 1 118 локальных ссылок,
  384 изображения, 163 JSON-LD и 81 URL Sitemap — ошибок и предупреждений нет;
- CSS полностью совпадает с `v24`, поэтому сетка, размеры карточек и
  адаптивные правила не менялись;
- исходный ZIP `v24` сохранил SHA-256
  `d32006b79551802b38a0f8f47e715ae18476a6948a1a7f247d783dd7ec9164ee`.

## Генерация изображения

Использован встроенный редактор изображений в режиме точного редактирования.
Исходная фотография применялась как edit target.

Основная инструкция:

> Нанести текст «Полимеры и реагенты» непосредственно на лицевую сторону
> большой белой банки как реалистичную тёмно-зелёную этикетку; сохранить
> тару, склад, свет, перспективу, небольшую карточку `PolymersPlus VIS` и
> чашу с гранулами без изменений; не добавлять отдельную угловую плашку и
> не изменять товарное изображение.

Публичный сайт не изменялся. `v25` подготовлена как отдельная сборка; `v24`
не перезаписывалась.
