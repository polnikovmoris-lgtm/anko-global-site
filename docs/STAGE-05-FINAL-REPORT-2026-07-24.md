# ANKO GLOBAL — Stage-05 SEO and Technical Optimization

Дата: 24.07.2026  
Статус: **COMPLETED LOCALLY / READY FOR STAGING PERFORMANCE VALIDATION**

## 1. База этапа

- Использована только подтверждённая сборка Stage-04 v8.
- Повторный регрессионный аудит перед изменениями не выявил битых ссылок, отсутствующих ресурсов, ошибок PHP/JSON-LD или несоответствий размеров.
- Публичная структура сохранена: 59 товаров, 8 категорий, 72 URL в sitemap.

## 2. SEO-оптимизация

### Индивидуальные Open Graph-превью

- Все 59 товарных страниц получили собственное изображение `og:image` вместо общего `og-image.jpg`.
- Для всех 59 товарных страниц задан `og:type=product`.
- Размер, MIME-тип и alt Open Graph-изображения определяются по фактическому локальному файлу.
- Добавлены `og:locale=ru_BY`, `og:image:alt` и `twitter:image:alt`.
- Категории получили собственные изображения для публикации в социальных сетях.

### Структурированные данные

- Каталог и 8 категорий получили Schema.org `CollectionPage` + `ItemList`.
- Количество элементов в ItemList соответствует фактическому каталогу:
  - каталог — 8 категорий;
  - бентониты — 6;
  - полимеры — 9;
  - установки — 10;
  - инструмент — 14;
  - Normet — 11;
  - смазки — 4;
  - локация — 3;
  - ПЭ-трубы — 2.
- Product и BreadcrumbList JSON-LD повторно проверены; ошибок синтаксиса нет.

### Sitemap, robots и канонизация

- В sitemap сохранены 72 публичных URL.
- Для всех 72 URL добавлен `lastmod=2026-07-24`.
- Добавлены 67 записей image sitemap: 59 товаров и 8 категорий.
- Добавлен постоянный 301-редирект `/index.php` → `/` для устранения дубля главной страницы.
- В robots.txt закрыты технические пути `/send.php`, `/thanks.php` и `/inc/`.
- `send.php` возвращает HTTP-заголовок `X-Robots-Tag: noindex, nofollow`.

### Семантика заголовков

- Заголовки групп футера заменены с H4 на нейтральные текстовые элементы.
- Формальные скачки уровней H1/H2→H4 устранены на всех страницах.
- На каждой публичной HTML-странице остаётся ровно один H1.

## 3. Производительность

### Изображения

- Размеры и визуальное содержание исходных изображений не изменялись.
- JPG-файлы перекодированы с консервативным качеством; экономия — 2 799 343 байта.
- Для 60 широких изображений созданы адаптивные WebP 640 и 960 px.
- Средний полный WebP: 54 586 байт.
- Средний мобильный WebP 640 px: 18 308 байт.
- Среднее уменьшение мобильной версии изображения: 66,5%.
- Из-за добавления адаптивных файлов общий архив изображений увеличился только на 299 593 байта.
- Все адаптивные варианты подключены через `srcset`/`sizes` и используются; неиспользуемых изображений нет.

### LCP и загрузка ресурсов

- Главная страница и 59 товарных страниц получили preload основного изображения — всего 60 страниц.
- Основные изображения имеют `fetchpriority=high`.
- Ленивые изображения получили `decoding=async`.
- Внешний Google Fonts полностью удалён.
- Используется системный шрифтовой стек без дополнительного сетевого запроса и без зависимости от третьей стороны.
- В `.htaccess` добавлена условная поддержка Brotli при наличии `mod_brotli`; Deflate сохранён как резерв.
- Версии CSS и JavaScript обновлены для корректного сброса браузерного кэша.

Размеры текстовых ресурсов после gzip:

- `style.css`: 9 946 байт;
- `main.js`: 2 805 байт;
- `search.js`: 11 346 байт.

## 4. Финальная техническая проверка

- Production-файлов: 351.
- PHP-файлов: 79.
- Товарных страниц: 59.
- Категорий: 8.
- Изображений и графических ресурсов: 265.
- Все 79 PHP-файлов проходят lint.
- JavaScript проходит `node --check`.
- CSS успешно разбирается PostCSS.
- Sitemap успешно разбирается XML-парсером.
- Проверено 262 растровых файла; повреждённых изображений нет.
- Битых внутренних ссылок: 0.
- Отсутствующих локальных ресурсов: 0.
- Несоответствий объявленных размеров изображений: 0.
- Изображений без alt: 0.
- Неиспользуемых изображений: 0.
- Дублирующихся бинарных изображений: 0.
- Ошибок JSON-LD: 0.
- Небезопасных `target=_blank` без `noopener noreferrer`: 0.
- Внешних запросов Google Fonts: 0.

HTTP-проверка локального сервера:

- 74 страницы — HTTP 200;
- `404.php` — HTTP 404;
- GET `send.php` — HTTP 405.

Проверка формы:

- неправильный CSRF — HTTP 419;
- неправильный телефон — HTTP 422;
- honeypot — безопасный HTTP 200 без отправки;
- корректная заявка при ненастроенной локальной почте — честный HTTP 503.

## 5. Ограничения и staging-gate

Локальная SEO- и техническая часть Stage-05 закрыта. На staging/production необходимо проверить:

1. Фактический 301-редирект `/index.php` → `/` и HTTPS/canonical-редиректы Apache.
2. Наличие `mod_brotli`, `mod_deflate`, `mod_expires` и `mod_headers` на хостинге.
3. Реальные заголовки HSTS, кэширования и безопасности.
4. Lighthouse/Core Web Vitals по публичному HTTPS URL.
5. Реальную доставку заявок через SMTP/транзакционную почту.
6. Аналитику и consent только после утверждения политики cookie.

## 6. Stage gate

**Stage-05 локально завершён.** Сборка готова для Stage-06: мобильная проверка, browser QA, Lighthouse и финальные тесты на staging/production.
